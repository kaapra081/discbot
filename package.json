{
  "name": "discord-sheet-reminder-bot",
  "version": "1.0.0",
  "private": true,
  "description": "A Discord bot that reads meetings from Google Sheets and sends configurable reminders.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/sheet.js && node --check src/reminders.js"
  },
  "dependencies": {
    "discord.js": "^14.21.0"
  }
}
